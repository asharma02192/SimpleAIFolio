# ──────────────────────────────────────────────
# SimpleAIFolio — Environment Configuration Template
# Copy to .env and customize for your deployment
# ──────────────────────────────────────────────

# Database
DB_PASSWORD=change-this-to-a-secure-password

# Security — generate with: openssl rand -hex 32
JWT_SECRET=change-this-to-a-random-secret
REVALIDATE_SECRET=change-this-to-a-random-secret

# Admin Account (created on first startup)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=change-this-password
SEED_ADMIN_NAME=Admin

# URLs (update for your domain in production)
NEXT_PUBLIC_API_URL=http://localhost:3201
NEXT_PUBLIC_SITE_URL=http://localhost:3200
FRONTEND_URL=http://localhost:3200

# AI Writer (optional — leave disabled to start)
# Configure from Admin > Settings > AI Configuration
# AI_PROVIDER=openai-compatible
# AI_API_KEY=your-openai-compatible-key
# AI_BASE_URL=https://api.openai.com/v1
# AI_MODEL=gpt-4o

# MCP Server (auto-deployed with Docker)
# API key is auto-generated — view it in Admin > Settings > MCP Server

# Public MCP endpoint shown in the admin MCP settings.
# Use this when MCP is exposed behind a reverse proxy.
# Example: https://mcp.example.com/mcp
# If empty, falls back to ${FRONTEND_URL}/mcp
MCP_PUBLIC_URL=

# MCP API timeout in milliseconds (for backend requests from MCP server).
# Default: 120000 (2 min). Increase if your AI provider is slow.
# Production recommendation: 300000 (5 min) as a safety fallback.
# The primary fix for timeouts is async draft generation (generate_draft + get_draft_status polling).
# MCP_API_TIMEOUT_MS=300000

# ── Production Only ──
# Name of the external Docker network your reverse proxy (nginx/Caddy) uses
# EXTERNAL_PROXY_NETWORK=proxy_default
